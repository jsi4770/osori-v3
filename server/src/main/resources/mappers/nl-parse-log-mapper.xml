<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="nlParseLogMapper">

	<insert id="insert" parameterType="NlParseLog">
		INSERT INTO NL_PARSE_LOG (
			LOG_ID,
			USER_ID,
			TYPE,
			INPUT_TEXT,
			SOURCE,
			RAW_LLM_JSON,
			RESULT_JSON,
			CONFIDENCE,
			CREATED_AT
		) VALUES (
			nextval('SEQ_NL_PARSE_LOG'),
			#{userId},
			#{type},
			#{inputText},
			#{source},
			#{rawLlmJson},
			#{resultJson},
			#{confidence},
			CURRENT_TIMESTAMP
		)
	</insert>

</mapper>
