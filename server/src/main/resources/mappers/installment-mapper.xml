<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="installmentMapper">

	<insert id="insertPlan" parameterType="InstallmentPlan" useGeneratedKeys="true" keyProperty="planId" keyColumn="plan_id">
		INSERT INTO INSTALLMENT_PLAN (
			PLAN_ID,
			USER_ID,
			TITLE,
			TOTAL_AMOUNT,
			INSTALLMENT_MONTHS,
			START_DATE,
			CATEGORY,
			MEMO
		) VALUES (
			nextval('SEQ_INSTALLMENT_PLAN'),
			#{userId},
			#{title},
			#{totalAmount},
			#{installmentMonths},
			#{startDate},
			#{category},
			#{memo}
		)
	</insert>

</mapper>
