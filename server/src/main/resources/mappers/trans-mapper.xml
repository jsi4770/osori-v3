<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="transMapper">

	<insert id="myTransSave" parameterType="Mytrans">
    INSERT INTO MYTRANS (
        TRAN_ID,
        TITLE,
        TRANS_DATE,
        ORIGINAL_AMOUNT,
        IS_SHARED,
        CATEGORY,
        TYPE,
        MEMO,
        USER_ID,
        GROUPB_ID,
        EXCLUDE_ANALYSIS,
        INSTALLMENT_ID)
        VALUES (
        nextval('SEQ_MYTRANS'),
        #{title},
        #{transDate},
        #{originalAmount},
        #{isShared},
        #{category},
        #{type},
        #{memo},
        #{userId},
        #{groupBId},
        #{excludeAnalysis},
        #{installmentId} )
</insert>

	<select id="selectMyTrans" resultType="mytrans">
	    SELECT
		    TRAN_ID        AS "transId",
		    TITLE          AS "title",
		    TRANS_DATE     AS "transDate",
		    ORIGINAL_AMOUNT AS "originalAmount",
		    CATEGORY       AS "category",
		    TYPE           AS "type",
		    MEMO           AS "memo",
		    USER_ID        AS "userId",
		    IS_SHARED      AS "isShared",
		    GROUPB_ID  	   AS "groupBId",
		    EXCLUDE_ANALYSIS AS "excludeAnalysis",
		    INSTALLMENT_ID AS "installmentId"
		FROM MYTRANS
		WHERE USER_ID = #{userId}
		ORDER BY TRAN_ID DESC
	</select>

	<update id="updateTrans" parameterType="mytrans">
		UPDATE MYTRANS
		SET
		TITLE = #{title},
		TRANS_DATE = #{transDate},
		ORIGINAL_AMOUNT = #{originalAmount},
		CATEGORY = #{category} ,
		TYPE = #{type},
		MEMO = #{memo},
		EXCLUDE_ANALYSIS = #{excludeAnalysis}
		WHERE TRAN_ID = #{transId}
		AND USER_ID = #{userId}
	</update>

	<update id="updateExcludeAnalysis">
		UPDATE MYTRANS
		SET EXCLUDE_ANALYSIS = #{excludeAnalysis}
		WHERE TRAN_ID = #{transId}
		AND USER_ID = #{userId}
	</update>

	<delete id="deleteTrans">
		DELETE FROM MYTRANS
		WHERE
		TRAN_ID = #{transId}
	</delete>

	<!-- [추가] 고정지출 -> MYTRANS 자동 반영 (매일 실행용) -->
	<!-- Postgres에는 표준 MERGE 버전 제약이 있어, INSERT ... SELECT ... WHERE NOT EXISTS로 동일 로직을 구현 -->
	<insert id="mergeFixedToMyTrans">
	  INSERT INTO MYTRANS (
	    TRAN_ID,
	    TITLE,
	    TRANS_DATE,
	    ORIGINAL_AMOUNT,
	    IS_SHARED,
	    CATEGORY,
	    TYPE,
	    MEMO,
	    USER_ID,
	    GROUPB_ID,
	    FIXED_ID
	  )
	  SELECT
	    nextval('SEQ_MYTRANS'),
	    f.NAME,
	    CURRENT_DATE,
	    f.AMOUNT,
	    'N',
	    f.CATEGORY,
	    'OUT',
	    '고정지출 자동등록',
	    f.USER_ID,
	    NULL,
	    f.FIXED_ID
	  FROM FIXEDTRANS f
	  WHERE
	    EXTRACT(DAY FROM CURRENT_DATE) =
	    LEAST(
	      f.PAY_DAY,
	      EXTRACT(DAY FROM (date_trunc('month', CURRENT_DATE) + INTERVAL '1 month - 1 day'))
	    )
	    AND NOT EXISTS (
	      SELECT 1 FROM MYTRANS m
	      WHERE m.FIXED_ID = f.FIXED_ID
	        AND m.TRANS_DATE = CURRENT_DATE
	        AND m.USER_ID = f.USER_ID
	    )
	</insert>

	<select id="recentTrans" resultType="mytrans">
	    SELECT
	        TITLE           AS "title",
	        ORIGINAL_AMOUNT  AS "originalAmount",
	        CATEGORY        AS "category",
	        TYPE            AS "type"
	    FROM MYTRANS
	    WHERE USER_ID = #{userId}
	      AND IS_SHARED = 'N'
	    ORDER BY TRAN_ID DESC
	    FETCH FIRST 3 ROWS ONLY
	</select>

</mapper>
